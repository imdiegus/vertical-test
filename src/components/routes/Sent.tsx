import React from 'react'
import Layout from '../commonComponents/Layout'

type Props = {}

export default function Sent({ }: Props) {
    return (
        <Layout>

            <div>Sent</div>
        </Layout>
    )
}